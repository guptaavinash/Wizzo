CREATE TABLE [dbo].[tblPrdActivePrdList] (
    [PrdNodeId]      INT NOT NULL,
    [PrdNodeType]    INT NOT NULL,
    [DistNodeId]     INT NOT NULL,
    [DistNodeType]   INT NOT NULL,
    [StockAvailable] INT NULL
);

